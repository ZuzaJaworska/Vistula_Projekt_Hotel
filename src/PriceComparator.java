import java.util.Comparator;

class PriceComparator implements Comparator<Room> {

    // szybki override metod z Comparator
    @Override
    public int compare(Room o1, Room o2) {
        return 0;
    }
}
